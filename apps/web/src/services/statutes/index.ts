/** Public API of the statute-documents layer — see `statuteDocuments.ts` for why this is a plain module, not a repository port. */
export {
  STATUTE_DOCUMENTS,
  getStatuteDocuments,
  getDocumentsForRuleSet,
  getDocumentsForCitationTopic,
} from './statuteDocuments'
