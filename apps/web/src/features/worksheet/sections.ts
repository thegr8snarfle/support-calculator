/**
 * DOM ids for the worksheet's section cards. Shared so the worksheet anchors and the
 * Review step's "Edit" scroll targets stay in sync.
 */
export const WORKSHEET_SECTIONS = {
  children: 'section-children',
  income: 'section-income',
  parenting: 'section-parenting',
  costs: 'section-costs',
} as const
